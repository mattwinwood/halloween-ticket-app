export const state = () => ({
})

export const mutations = {
}
export const actions = {
  // nuxtServerInit ({ dispatch }, context) {
  //   return Promise.all([
  //                        dispatch('user/fetchUserRoles', context)
  //                      ])
  // }
}
